# HoneyShield dashboard — bound to localhost only, per
# HONEYSHIELD_PROJECT.md section 6 point 2: "Never expose the dashboard.
# Bind Streamlit to 127.0.0.1 only." Access it via SSH tunnel or
# Cloudflare Tunnel + Access when deployed — never a public port.

[server]
address = "127.0.0.1"
headless = true
# Off — this was the single largest cost in the console, measured, not assumed.
#
# The watcher exists to hot-reload the app when you edit a source file. To know
# what to watch, Streamlit walks EVERY module in sys.modules after each script
# run of each browser session, resolving and glob-matching every path — and it
# does this ON THE SERVER'S EVENT LOOP. Instrumented against the live app:
# 1.6-2.3 s per new session (1,561 modules -> 2,150 once google.genai loads),
# during which the finished page could not be delivered to that browser, nor
# any output to any OTHER open session. That was most of the 3+ seconds it took
# even the sign-in form to appear.
#
# Cost of turning it off: editing a .py file no longer triggers a reload. Restart
# the server after code changes — which a running console should require anyway.
fileWatcherType = "none"

[browser]
gatherUsageStats = false

[client]
# Hide Streamlit's own toolbar (the "Deploy" button and hamburger menu).
# Those exist to push apps to Streamlit Community Cloud, which is the exact
# opposite of this app's threat model — it must never leave localhost.
toolbarMode = "minimal"
# Off, and this cannot be done with CSS. Streamlit's frontend paints the page
# nav as soon as the browser connects — before the script has produced any
# output — so a stylesheet injected by st.markdown always loses that race and
# the raw menu flashes on the login screen. Disabling it at config level means
# the frontend never renders it. Navigation is rebuilt with st.page_link in
# dashboard/login.py, and only for authenticated sessions.
showSidebarNavigation = false

# Base palette. The rest of the design system lives in dashboard/theme.py;
# these are the values Streamlit needs natively to paint its own widgets
# (inputs, sliders, selects) so they don't fight the custom CSS.
[theme]
base = "dark"
primaryColor = "#F5A524"
backgroundColor = "#0B0F17"
secondaryBackgroundColor = "#131A27"
textColor = "#E6EAF2"
font = "sans serif"
